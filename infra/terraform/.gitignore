# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# 변수 값(민감정보 가능) — 예시 파일(*.tfvars.example)은 커밋
*.tfvars
*.tfvars.json

override.tf
override.tf.json
*_override.tf
*_override.tf.json

.terraformrc
terraform.rc
